<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE struts PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"     "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>

	<!-- Master Setup -->

	<package name="AppSetup" extends="struts-default" namespace="/AppSetup">
		<result-types>
			<result-type name="tiles"
				class="org.apache.struts2.views.tiles.TilesResult" />
		</result-types>

		<interceptors>
			<interceptor name="CheckSession"
				class="framework.core.custom.interceptors.CheckSession"></interceptor>
			<interceptor-stack name="CheckSession">
				<interceptor-ref name="CheckSession" />
				<interceptor-ref name="defaultStack" />
			</interceptor-stack>
		</interceptors>

		<action name="ServerTime">
			<result name="success">/pages/tiles/global/ServerTime.jsp
			</result>
		</action>

		<action name="WeekDays" class="app.actions.session.on.admin.AdminAction"
			method="execute">
			<result name="error">App.Landing</result>
			<result type="tiles" name="success">Setup.weekDays</result>
		</action>
		
		
		<action name="ProcessWeekdaySettings" class="app.actions.session.on.admin.AdminAction"
			method="processWeekdaySettings">
			<result name="error">App.Landing</result>
			<result type="tiles" name="success">Setup.weekDays</result>
		</action>

	</package>

</struts> 